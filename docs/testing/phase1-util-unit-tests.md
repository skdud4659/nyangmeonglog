# 🎓 Phase 1: 유틸 단위 테스트 학습 가이드

## 📊 테스트 결과 요약

- **총 테스트 파일**: 2개
- **총 테스트 케이스**: 18개 (모두 통과 ✅)
- **테스트 대상 함수**: 3개
  - `formatDateKorean` - 5개 테스트
  - `getDday` - 7개 테스트
  - `urlBase64ToUint8Array` - 6개 테스트

---

# 📁 테스트 1: 날짜 유틸 함수

## 🎯 테스트 대상

`src/shared/lib/date.ts`의 날짜 처리 유틸 함수

## 📝 작성한 테스트 케이스

### 1️⃣ formatDateKorean 함수 (5개 테스트)

**함수 역할**: ISO 날짜 문자열(예: "2025-08-04")을 한글 형식(예: "8월 4일")으로 변환

**테스트 1: 기본 동작**

```typescript
it('ISO 날짜를 한글 형식으로 변환한다', () => {
    const result = formatDateKorean('2025-08-04');
    expect(result).toBe('8월 4일');
});
```

**학습 포인트**

- **AAA 패턴** (Arrange-Act-Assert)
  - **Arrange (준비)**: 테스트 데이터 준비 (`const isoDate = '2025-08-04'`)
  - **Act (실행)**: 함수 실행 (`formatDateKorean(isoDate)`)
  - **Assert (검증)**: 결과 확인 (`expect(result).toBe(...)`)
- `toBe()` matcher: 정확한 값 비교 (원시 타입용)

---

**테스트 2-4: 경계값 테스트**

```typescript
it('1월 1일을 올바르게 변환한다')  // 최소값
it('12월 31일을 올바르게 변환한다') // 최대값
it('2월 29일(윤년)을 올바르게 변환한다') // 특수 케이스
```

**학습 포인트**

- **경계값 테스트 (Boundary Testing)**: 극단적인 값에서 함수가 잘 동작하는지 확인
- 1월 1일, 12월 31일처럼 최소/최대 경계값 테스트
- 윤년 같은 특수한 경우 테스트

---

**테스트 5: 다양한 입력 처리**

```typescript
it('한 자리 월과 한 자리 일을 올바르게 처리한다', () => {
    const result = formatDateKorean('2025-03-05');
    expect(result).toBe('3월 5일'); // "03월 05일"이 아님!
});
```

**학습 포인트**

- **포맷팅 검증**: 앞자리 0이 제거되는지 확인
- 실제 사용자에게 보이는 형식 테스트

---

### 2️⃣ getDday 함수 (7개 테스트)

**함수 역할**: 특정 날짜까지의 D-Day 계산 (D-Day, D-5, D+3 등)

**시간 모킹 설정**

```typescript
beforeEach(() => {
    vi.useFakeTimers();
    vi.setSystemTime(new Date('2025-11-04'));
});

afterEach(() => {
    vi.useRealTimers();
});
```

**학습 포인트**

- **시간 모킹 (Time Mocking)**: `vi.useFakeTimers()`로 현재 시간 고정
- **beforeEach / afterEach**: 각 테스트 전/후에 실행되는 훅
- 시간에 의존하는 함수를 테스트할 때 필수!
- `vi.setSystemTime()`: 현재 시간을 2025-11-04로 고정

---

**주요 테스트 케이스**

```typescript
it('당일이면 "D-Day"를 반환한다', () => {
    const result = getDday('2025-11-04');
    expect(result).toBe('D-Day');
});

it('미래 날짜는 "D-n" 형식으로 반환한다', () => {
    const result = getDday('2025-11-10');
    expect(result).toBe('D-6');
});

it('과거 날짜는 "D+n" 형식으로 반환한다', () => {
    const result = getDday('2025-10-30');
    expect(result).toBe('D+5');
});
```

**학습 포인트**

- **조건 분기 테스트**: 모든 경우의 수를 테스트
  - 당일 (D-Day)
  - 미래 (D-n)
  - 과거 (D+n)
- **엣지 케이스**: D-1, D+1처럼 경계값 테스트

---

**큰 숫자 테스트**

```typescript
it('1년 후를 올바르게 계산한다', () => {
    const result = getDday('2026-11-04');
    expect(result).toBe('D-365');
});
```

**학습 포인트**

- **큰 숫자 테스트**: 1년 = 365일 계산이 정확한지 확인
- 윤년 고려 여부 파악 가능

---

# 📁 테스트 2: Base64 인코딩 함수

## 🎯 테스트 대상

`src/shared/lib/push.ts`의 `urlBase64ToUint8Array` 함수

**함수 역할**: URL-safe Base64 문자열을 `Uint8Array`로 변환 (Web Push의 VAPID 키 처리용)

## 📝 작성한 테스트 케이스 (6개)

**테스트 1: 기본 변환**

```typescript
it('URL-safe Base64 문자열을 Uint8Array로 변환한다', () => {
    const base64String = 'QUJD'; // "ABC"의 Base64
    const result = urlBase64ToUint8Array(base64String);

    expect(result).toBeInstanceOf(Uint8Array);
    expect(result.length).toBe(3);
    expect(result[0]).toBe(65); // 'A'의 ASCII 코드
    expect(result[1]).toBe(66); // 'B'
    expect(result[2]).toBe(67); // 'C'
});
```

**학습 포인트**

- **타입 검증**: `toBeInstanceOf(Uint8Array)` - 반환 타입 확인
- **배열 요소 검증**: 각 바이트 값이 정확한지 확인
- **ASCII 코드 이해**: 'A' = 65, 'B' = 66, 'C' = 67

---

**테스트 2: 패딩 처리**

```typescript
it('패딩이 필요한 경우 자동으로 추가한다', () => {
    const base64String = 'QUI'; // 길이가 3 -> 패딩 필요
    const result = urlBase64ToUint8Array(base64String);

    expect(result.length).toBe(2);
});
```

**학습 포인트**

- **Base64 패딩 규칙**: Base64 문자열은 4의 배수여야 함
- 부족한 경우 `=` 문자로 패딩 추가
- 함수가 자동으로 패딩을 처리하는지 검증

---

**테스트 3: URL-safe 문자 변환**

```typescript
it('URL-safe 문자(-, _)를 표준 Base64(+, /)로 변환한다', () => {
    const urlSafeBase64 = 'Pz4-'; // URL-safe 형식
    const result = urlBase64ToUint8Array(urlSafeBase64);

    expect(result[0]).toBe(63); // '?'
});
```

**학습 포인트**

- **URL-safe Base64 이해**:
  - 표준 Base64: `+`, `/` 사용
  - URL-safe: `-`, `_` 사용 (URL에 안전한 문자)
- 함수가 URL-safe → 표준 변환을 올바르게 하는지 검증

---

**테스트 4: 엣지 케이스**

```typescript
it('빈 문자열을 처리한다', () => {
    const result = urlBase64ToUint8Array('');
    expect(result.length).toBe(0);
});
```

**학습 포인트**

- **엣지 케이스**: 빈 입력 처리
- 함수가 크래시 없이 빈 배열을 반환하는지 확인

---

**테스트 5: 실제 사용 케이스**

```typescript
it('긴 VAPID 키 형식의 문자열을 올바르게 변환한다', () => {
    const longBase64 = 'BEl62iUYgUivxIkv69yViEuiBIa-Ib27SUrSk3K1IhHDB3_UPIQzCYU';
    const result = urlBase64ToUint8Array(longBase64);

    expect(result.length).toBeGreaterThan(0);
});
```

**학습 포인트**

- **실제 사용 케이스**: 실제 VAPID 키 형식 테스트
- 긴 문자열 처리 검증

---

# 🎓 핵심 학습 내용 정리

## 1. 테스트 기본 문법

```typescript
describe('함수/기능 이름', () => {
    it('무엇을 테스트하는지 명확하게', () => {
        // Arrange (준비)
        const input = '테스트 데이터';

        // Act (실행)
        const result = myFunction(input);

        // Assert (검증)
        expect(result).toBe('예상 결과');
    });
});
```

- `describe`: 테스트 그룹화 (Test Suite)
- `it`: 개별 테스트 케이스 (Test Case)
- `expect`: 결과 검증 (Assertion)

---

## 2. Matcher 종류

| Matcher | 용도 | 예시 |
| --- | --- | --- |
| `toBe()` | 정확한 값 비교 (원시 타입) | `expect(5).toBe(5)` |
| `toBeInstanceOf()` | 타입 검증 | `expect(arr).toBeInstanceOf(Array)` |
| `toBeGreaterThan()` | 크기 비교 | `expect(10).toBeGreaterThan(5)` |

---

## 3. 테스트 훅 (Hooks)

```typescript
beforeEach(() => {
    // 각 테스트 전에 실행
});

afterEach(() => {
    // 각 테스트 후에 실행
});
```

---

## 4. 모킹 (Mocking)

```typescript
vi.useFakeTimers();          // 시간 모킹 시작
vi.setSystemTime(new Date()); // 시간 고정
vi.useRealTimers();           // 시간 모킹 종료
```

---

# 💡 테스트 작성 전략

## ✅ 좋은 테스트의 조건

1. **명확한 테스트 이름**: 무엇을 테스트하는지 한눈에 파악
2. **하나의 테스트, 하나의 검증**: 한 테스트에서 여러 것을 테스트하지 않기
3. **독립성**: 각 테스트는 다른 테스트에 영향을 주지 않음
4. **반복 가능**: 언제 실행해도 같은 결과

---

## 📋 테스트해야 할 것들

### 1. 정상 케이스 (Happy Path)

일반적인 입력에 대한 올바른 출력

### 2. 경계값 (Boundary Values)

최소값, 최대값, 특수한 값

### 3. 엣지 케이스 (Edge Cases)

빈 문자열, null, 0 등

### 4. 예외 처리 (Error Handling)

잘못된 입력에 대한 처리 (`toThrow()` 사용)

---

# 🚀 스터디 발제 주제 제안

## 1. 시간 의존 함수 테스트의 중요성

**주제**: "왜 `getDday` 테스트에서 시간을 고정해야 할까?"

**발제 내용**:

- 시간에 의존하는 함수의 테스트 어려움
- `vi.useFakeTimers()`를 사용한 시간 모킹
- 실무에서 시간 관련 버그 사례 (타임존, 서머타임 등)

**토론 질문**:

- 시간을 모킹하지 않으면 어떤 문제가 발생할까?
- 실제 프로덕션 코드에서 시간 처리 시 주의할 점은?

---

## 2. 경계값 테스트의 실전 활용

**주제**: "1월 1일, 12월 31일, 윤년... 왜 이런 값들을 테스트해야 할까?"

**발제 내용**:

- 경계값 테스트가 실제 버그를 찾아낸 사례
- `formatDateKorean`에서 12월 31일을 테스트한 이유
- Off-by-one 에러 (배열 인덱스, 날짜 계산 등)

**토론 질문**:

- 여러분의 코드에서 경계값 버그를 경험한 적이 있나요?
- 어떤 경계값들을 테스트해야 할까요?

---

## 3. Base64와 인코딩 이해하기

**주제**: "`urlBase64ToUint8Array` 함수는 정확히 무엇을 하는 걸까?"

**발제 내용**:

- Base64 인코딩의 원리
- URL-safe Base64가 필요한 이유
- Web Push에서 VAPID 키 처리 과정
- `Uint8Array`란 무엇인가?

**토론 질문**:

- 왜 브라우저에서 바이너리 데이터를 다룰 때 `Uint8Array`를 사용할까?
- Base64 패딩(`=`)은 왜 필요할까?

---

# 📚 추가 학습 자료

## 다음 단계로 학습할 내용

### 1. 예외 처리 테스트

```typescript
it('잘못된 날짜 형식은 에러를 발생시킨다', () => {
    expect(() => formatDateKorean('invalid')).toThrow();
});
```

### 2. 테스트 커버리지

```bash
npm run test:coverage
```

코드의 몇 %가 테스트되었는지 확인

### 3. UI 테스트

Vitest UI로 시각적으로 테스트 결과 확인

```bash
npm run test:ui
```

---

# ✅ 완료된 작업

1. ✅ Vitest 설치 및 설정
2. ✅ `vitest.config.ts` 생성
3. ✅ 날짜 유틸 함수 12개 테스트 작성
4. ✅ Base64 변환 함수 6개 테스트 작성
5. ✅ 총 18개 테스트 모두 통과
6. ✅ package.json에 테스트 스크립트 추가

---

# 🎯 실행 명령어

```bash
# 테스트 실행
npm test

# UI로 테스트 확인
npm run test:ui

# 커버리지 확인
npm run test:coverage
```

---

# 📂 테스트 파일 구조

```
src/shared/lib/
├── __tests__/
│   ├── date.test.ts       # 날짜 유틸 함수 테스트 (12개)
│   └── push.test.ts       # Base64 변환 함수 테스트 (6개)
├── date.ts                # 테스트 대상: formatDateKorean, getDday
└── push.ts                # 테스트 대상: urlBase64ToUint8Array
```

---

**작성일**: 2025-11-12
**Phase**: 1 - 유틸 단위 테스트
**다음 Phase**: 2 - 컴포넌트 테스트 (예정)
